apiVersion: apps/v1
kind: Deployment
metadata:
  name: car-ui-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      component: car-ui
  template:
    metadata:
      labels:
        component: car-ui
    spec:
      containers:
        - name: car-ui-container
          image: localhost:5000/car-owner-ui
          ports:
            - containerPort: 80